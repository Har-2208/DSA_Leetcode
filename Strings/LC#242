bool isAnagram(char* s, char* t) {
    int f[26] = {0}; 
    int flag=0;
    if(strlen(s)!=strlen(t)){
            return false;
    }
    for(int i=0; i<strlen(s); i++){
       f[s[i]- 'a']++;
    }
    for(int i=0; i<strlen(t); i++){
       f[t[i] - 'a']--;
    }
    for(int i=0; i<26; i++){
        if(f[i]!=0){
         return false;   
        }
    }
    return true;
}