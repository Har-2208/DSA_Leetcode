int lengthOfLastWord(char* s) {
    int k = strlen(s) - 1;
    int i = 0;

    while (k >= 0 && s[k] == ' ') {
        k--;
    }

    while (k >= 0 && s[k] != ' ') {
        i++;
        k--;
    }

    return i;
}
